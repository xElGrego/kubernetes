apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: ecommerce-platform
  labels:
    app: users-api
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
        tier: backend
    spec:
      containers:
      - name: users-api
        image: users-api:latest
        imagePullPolicy: Never  # Para desarrollo local con Minikube/Docker Desktop
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: ENVIRONMENT
          value: "kubernetes"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
